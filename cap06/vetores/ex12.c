#include <stdio.h>

int main(){

    int vetor[10], i;

    printf("Insira 10 valores (pode repetir valores):\n");
    for(i=0;i<10;i++){
        scanf("%d", &vetor[i]);
    }

    for(i=0;i<10;i++){
        if(vetor[i]){

        }
    }

    return 0;
}